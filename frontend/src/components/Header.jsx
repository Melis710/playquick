
import React from "react";

const Header = () => (
  <header className="main-header">
    <h1>Welcome to PlayQuick</h1>
    <p>Instant fun with mini-games — no downloads, no sign-ups, play now!</p>
  </header>
);

export default Header;